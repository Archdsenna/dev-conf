Q: 如何将本地仓库上传到github?
A:  (1) 先在github上新建仓库
    (2) 初始化本地仓库: 
            $ git init
    (3) 关联远程仓库:
            $ git remote add origin GITHUB-URL
    (4) 推送本地仓库到github
            $ git push -u origin master

